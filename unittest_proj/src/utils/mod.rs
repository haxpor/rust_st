/// mod.rs is the module file.
/// We have two options
/// 1. Create module file at the root directory in src/ with the same name of module
/// 2. Create mod.rs file at the sub-directory of such module name we desire
///
/// In this case, clearly we chose option 2.
pub mod dummy_val;
